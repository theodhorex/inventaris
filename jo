def push(self, e): #sama dengan tambah depan
        baru = Node(e, None)
        if self.isEmpty() :
            self._head = baru
            self._head._next = None
        else :
            baru._next = self._head
            self._head = baru
        self._size += 1
